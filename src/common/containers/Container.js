import styled from "styled-components";

export default function Container() {
  return <StyledContainer></StyledContainer>;
}

const StyledContainer = styled.div`
  padding: 20px;
  box-shadow: 0px 0px 19px -1px rgba(0, 0, 0, 0.74);
  border-radius: 4px;
`;
